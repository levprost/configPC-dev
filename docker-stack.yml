services:
  laravel-docker:
    image: ghcr.io/levprost/ConfigPC-dev:latest
    ports:
      - 8000:80

  vue-docker:
    image: ghcr.io/levprost/ConfigPC-dev:latest
    ports:
      - 80:80

  mysql_db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: laravel
    ports:
      - 3306:3306